# FindSubdomainWithSubfinderNAssetfinder
This script was AI-generated. I created it to fulfill my own needs and set it to public in case it might be useful to someone else.这个脚本是ai生成的，我是因为自己有需求才做出这个的，设置为pulic是想着可能有人可以用上。

# Basic Usage
```./recon.sh example.com results.txt```

# Custom Configuration
```HTTPX_THREADS=100 HTTPX_TIMEOUT=5 ./recon.sh example.com results.txt```

# All Configuration Options
```HTTPX_THREADS=100 HTTPX_TIMEOUT=5 HTTPX_RETRIES=3 MAX_PARALLEL_JOBS=4 ./recon.sh example.com results.txt```

# 基本用法
```./recon.sh example.com results.txt```

# 自定义配置
```HTTPX_THREADS=100 HTTPX_TIMEOUT=5 ./recon.sh example.com results.txt```

# 所有配置选项
```HTTPX_THREADS=100 HTTPX_TIMEOUT=5 HTTPX_RETRIES=3 MAX_PARALLEL_JOBS=4 ./recon.sh example.com results.txt```
